version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: pets-mysql
    restart: always
    ports:
      - '3306:3306'
    environment: Prakash@1
      MYSQL_ROOT_PASSWORD: 
      MYSQL_DATABASE: pets_shelter
      MYSQL_USER: root
      MYSQL_PASSWORD: prakash@1
